"""Setup for GFMM"""

from distutils.core import setup

setup(
    name='vat-formatter',
    version='0.0.1',
    description='Takes data and labels from VAT and transforms them into a different format.',
    author='Lawrence Thatcher',
    author_email='lwthatcher@msn.com',
    url='https://github.com/lwthatcher/mlp',
    packages=['formatter']
)